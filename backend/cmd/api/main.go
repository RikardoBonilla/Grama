package main

import "log"

// Sprint 0 placeholder — real wiring (DB, router, graceful shutdown) in Sprint 1.
// main.go is the composition root: reads config, builds dependencies, starts server.
// Business logic NEVER lives here.
func main() {
	log.Println("Grama API starting — Sprint 0 placeholder")
}
