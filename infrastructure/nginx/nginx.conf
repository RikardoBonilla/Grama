# TODO Sprint 4: configuración de Nginx para producción.
# Controles de seguridad obligatorios:
#   - TLS 1.2/1.3 únicamente (deshabilitar TLS 1.0 y 1.1)
#   - HSTS (Strict-Transport-Security: max-age=31536000; includeSubDomains)
#   - Rate limiting (limit_req_zone) para /api/v1/login y /api/v1/register
#   - Proxy hacia el backend Go en puerto 8080
#   - Bloquear acceso a rutas internas (/_internal/, /metrics, etc.)
#   - Logs de acceso con formato estándar (sin datos sensibles)
