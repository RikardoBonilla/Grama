// TODO Sprint 1: variables de entorno para DESARROLLO LOCAL.
// NUNCA poner secretos aquí — este archivo SÍ va a git.
// Los secretos reales van en .env.local (gitignored).

export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api/v1',
};
